module Main (main) where

import Advent (input)

main :: IO ()
main = do
    i <- $(input)
    print $ p1 i
    print $ p2 i

p1 :: String -> Int
p1 = length . lines

p2 :: String -> Int
p2 = length . lines
